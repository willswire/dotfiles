[user]
	name = William Walker

[alias]
	amend = commit --all --amend --no-edit
	please = push --force-with-lease
    play = "!f() { \
        git config user.email 'w_walker@icloud.com' && \
        git config user.signingkey '0BD90D90'; \
    }; f"
    work = "!f() { \
        git config user.email 'william@defenseunicorns.com' && \
        git config user.signingkey '39233F36'; \
    }; f"

[fetch]
	prune = true

[commit]
	gpgsign = true

[gpg]
	program = /usr/local/bin/gpg

[init]
	defaultBranch = main

[push]
	autoSetupRemote = true

[credential]
	helper = osxkeychain
